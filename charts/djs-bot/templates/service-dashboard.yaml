apiVersion: v1
kind: Service
metadata:
  name: {{ include "djs-bot.fullname" . }}"-" .Values.dashboard.deployment-name }}
  labels:
    {{- include "djs-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard..service.type }}
  ports:
    - port: {{ .Values.dashboard..service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djs-bot.dashboard.selectorLabels" . | nindent 4 }}
