apiVersion: v1
kind: Service
metadata:
  name: {{ include "djs-bot.fullname" . }}
  labels:
    {{- include "djs-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.djs-bot..service.type }}
  ports:
    - port: {{ .Values.djs-bot..service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djs-bot.selectorLabels" . | nindent 4 }}
