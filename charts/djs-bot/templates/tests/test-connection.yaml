apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djsBot.fullname" . }}-test-connection"
  labels:
    {{- include "djsBot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djsBot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
