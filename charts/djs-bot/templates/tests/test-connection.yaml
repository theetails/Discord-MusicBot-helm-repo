apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djs-bot.fullname" . }}-test-connection"
  labels:
    {{- include "djs-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djs-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
