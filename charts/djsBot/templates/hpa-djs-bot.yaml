{{- if .Values.djsBot.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "djsBot.fullname" . }}
  labels:
    {{- include "djsBot.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "djsBot.fullname" . }}
  minReplicas: {{ .Values.djsBot.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.djsBot.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.djsBot.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.djsBot.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.djsBot.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.djsBot.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
