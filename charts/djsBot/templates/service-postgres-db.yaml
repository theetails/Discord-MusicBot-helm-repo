apiVersion: v1
kind: Service
metadata:
  name: {{ include "djsBot.fullname" . }}"-" .Values.postgresDb.deploymentName }}
  labels:
    {{- include "djsBot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgresDb.service.type }}
  ports:
    - port: {{ .Values.postgresDb.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "djsBot.postgresDb.selectorLabels" . | nindent 4 }}
